# requirements-pi.txt
# For Raspberry Pi 3B (Bullseye 64-bit, Python 3.11)
# Install with: pip install -r requirements-pi.txt --break-system-packages
ai-edge-litert
scikit-learn==1.3.2
joblib==1.3.2
numpy==1.26.4
fastapi==0.115.0
uvicorn==0.30.6
jinja2==3.1.4
websockets==12.0
python-multipart==0.0.9
# NOTE: scikit-learn version MUST match the training machine exactly.
# Models serialized with joblib on one version may fail on another.
# Do NOT install tensorflow on the Pi -- use ai-edge-litert instead.
